<!DOCTYPE html>
<html lang="en">
<head th:replace="fragments/head" />
<body>

	<header th:replace="fragments/nav" />
	<div class="container">
		<h2>Lista de peticiones recibidas</h2>
		<p>Los usuarios que actualmente figuran en el sistema son los
			siguientes:</p>


		<div class="table-responsive">
			<table class="table table-hover">
				<thead>
					<tr>
						<th>EMAIL</th>
						<th>Nombre</th>
						<th>Apellidos</th>
						<th class="col-md-1"></th>
						<th class="col-md-1"></th>
						<th class="col-md-1"></th>
					</tr>
				</thead>
				<tbody>
					<tr th:each="user : ${usersList}" th:object="${user}"
					    th:if="${user.email} != ${#authentication.getPrincipal().getUsername()}"  >
						<td th:text="${user.email}">71888888X</td>
						<td th:text="${user.name}">Nombre del alumno</td>
						<td th:text="${user.lastName}">Apellidos del alumno</td>
						
						<td th:if="${#bools.isFalse(#lists.contains(peticionsList, user.id))}">
						<button type=" button" th:id="${'peticionButton' + user.id}" class="btn btn-if">AÑADIR AMIGO
								</button> 
								<script th:inline="javascript">
									/*<![CDATA[*/
									$("#peticionButton[[${user.id}]]")
											.click(function() { 
												$.get( "/user/[[${user.id}]]/makePeticion",function(data) {
													 var numberPage = [[${param.page}]];
													 var urlUpdate = '/user/list/update';
													 if ( numberPage != null ){
													 urlUpdate += "?page="+numberPage[0];
													 }
													$("#tableUser").load(urlUpdate);
													
													
												});
												$("#peticionButton[[${user.id}]]").attr('disabled', true);
											});
									/*]]>*/
								</script>
						</td>
						<td th:unless="${#bools.isFalse(#lists.contains(peticionsList, user.id))}">
							<button type=" button" th:id="${'peticionButton' + user.id}" class="btn btn-warning">PETICIÓN ENVIADA
								</button>
							<script th:inline="javascript">
									/*<![CDATA[*/
									$("#peticionButton[[${user.id}]]").attr('disabled', true);
									/*]]>*/
							</script>
						</td>
						
					</tr>
				</tbody>
			</table>
		</div>
		<footer th:replace="fragments/pagination" />
	</div>
	<footer class="footer" th:replace="fragments/footer" />
</body>
</html>
